---
export interface Props {
  title: string;
  description?: string;
}

const { title, description = "Alejandro Parodi - Offensive Security Specialist | Red Team | CVE Research | Offensive Tooling" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=DM+Mono:ital,wght@0,400;0,500;1,400;1,500&display=swap" rel="stylesheet">
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  /* Custom CSS Variables */
  :root {
    --color-dark-primary: #0f111a;
    --color-dark-secondary: #181b2a;
    --color-volt: #57fd6b;
    --color-volt-soft: rgba(87, 253, 107, 0.15);
    --color-text-primary: #f7f8fa;
    --color-text-secondary: rgba(247, 248, 250, 0.7);
  }

  /* Reset and base styles */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    font-family: "DM Sans", sans-serif;
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--color-dark-primary);
    color: var(--color-text-primary);
    font-feature-settings: "kern" 1, "liga" 1, "calt" 1;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    min-height: 100vh;
  }

  /* Custom Tailwind utilities */
  .bg-dark-primary {
    background-color: var(--color-dark-primary) !important;
  }

  .bg-dark-secondary {
    background-color: var(--color-dark-secondary) !important;
  }

  .text-text-primary {
    color: var(--color-text-primary) !important;
  }

  .text-text-secondary {
    color: var(--color-text-secondary) !important;
  }

  .text-volt {
    color: var(--color-volt) !important;
  }

  .border-volt {
    border-color: var(--color-volt) !important;
  }

  /* Custom utility classes for volt colors with opacity */
  .border-volt\/10 { border-color: rgba(87, 253, 107, 0.1) !important; }
  .border-volt\/20 { border-color: rgba(87, 253, 107, 0.2) !important; }
  .border-volt\/30 { border-color: rgba(87, 253, 107, 0.3) !important; }
  .border-volt\/40 { border-color: rgba(87, 253, 107, 0.4) !important; }

  .bg-volt\/5 { background-color: rgba(87, 253, 107, 0.05) !important; }
  .bg-volt\/10 { background-color: rgba(87, 253, 107, 0.1) !important; }
  .bg-volt\/15 { background-color: rgba(87, 253, 107, 0.15) !important; }
  .bg-volt\/20 { background-color: rgba(87, 253, 107, 0.2) !important; }
  .bg-volt\/30 { background-color: rgba(87, 253, 107, 0.3) !important; }

  .text-volt\/80 { color: rgba(87, 253, 107, 0.8) !important; }
  .text-volt\/90 { color: rgba(87, 253, 107, 0.9) !important; }

  /* Emerald utilities */
  .bg-emerald-400\/10 { background-color: rgba(74, 222, 128, 0.1) !important; }
  .bg-emerald-400\/15 { background-color: rgba(74, 222, 128, 0.15) !important; }
  .bg-emerald-400\/20 { background-color: rgba(74, 222, 128, 0.2) !important; }
  .border-emerald-400\/30 { border-color: rgba(74, 222, 128, 0.3) !important; }
  .border-emerald-400\/40 { border-color: rgba(74, 222, 128, 0.4) !important; }
  .text-emerald-400 { color: #4ade80 !important; }

  /* Background gradients */
  .bg-gradient-radial {
    background: radial-gradient(circle, var(--tw-gradient-stops));
  }

  /* Shadows and glows */
  .drop-shadow-volt {
    filter: drop-shadow(0 0 8px rgba(87,253,107,0.5));
  }

  .shadow-volt-soft {
    box-shadow: 0 0 20px rgba(87,253,107,0.3);
  }

  .shadow-volt-glow {
    box-shadow: 0 0 40px rgba(87,253,107,0.6);
  }

  /* Animations */
  @keyframes neon-slide {
    0% { left: 0%; opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 1; }
    100% { left: calc(100% - 8px); opacity: 0; }
  }

  .animate-neon-slide {
    animation: neon-slide 3s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  @keyframes bounce {
    0%, 100% {
      transform: translateY(-25%);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    50% {
      transform: none;
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
  }

  .animate-bounce {
    animation: bounce 1s infinite;
  }

  /* Responsive utilities */
  .container {
    width: 100%;
    margin-left: auto;
    margin-right: auto;
    padding-left: 1rem;
    padding-right: 1rem;
  }

  @media (min-width: 640px) {
    .container { max-width: 640px; }
  }

  @media (min-width: 768px) {
    .container { max-width: 768px; }
  }

  @media (min-width: 1024px) {
    .container { max-width: 1024px; }
  }

  @media (min-width: 1280px) {
    .container { max-width: 1280px; }
  }

  @media (min-width: 1536px) {
    .container { max-width: 1536px; }
  }

  /* Common Tailwind utilities */
  .flex { display: flex; }
  .inline-flex { display: inline-flex; }
  .block { display: block; }
  .inline-block { display: inline-block; }
  .hidden { display: none; }
  .grid { display: grid; }

  .relative { position: relative; }
  .absolute { position: absolute; }
  .fixed { position: fixed; }

  .top-0 { top: 0; }
  .bottom-0 { bottom: 0; }
  .left-0 { left: 0; }
  .right-0 { right: 0; }
  .inset-0 { top: 0; right: 0; bottom: 0; left: 0; }

  .z-10 { z-index: 10; }
  .z-50 { z-index: 50; }

  .w-full { width: 100%; }
  .w-auto { width: auto; }
  .w-1 { width: 0.25rem; }
  .w-1\.5 { width: 0.375rem; }
  .w-2 { width: 0.5rem; }
  .w-4 { width: 1rem; }
  .w-6 { width: 1.5rem; }
  .w-8 { width: 2rem; }
  .w-10 { width: 2.5rem; }
  .w-12 { width: 3rem; }
  .w-16 { width: 4rem; }
  .w-96 { width: 24rem; }
  .w-80 { width: 20rem; }

  .h-1 { height: 0.25rem; }
  .h-1\.5 { height: 0.375rem; }
  .h-2 { height: 0.5rem; }
  .h-3 { height: 0.75rem; }
  .h-4 { height: 1rem; }
  .h-6 { height: 1.5rem; }
  .h-8 { height: 2rem; }
  .h-10 { height: 2.5rem; }
  .h-12 { height: 3rem; }
  .h-16 { height: 4rem; }
  .h-96 { height: 24rem; }
  .h-80 { height: 20rem; }

  .min-h-screen { min-height: 100vh; }

  .max-w-3xl { max-width: 48rem; }
  .max-w-4xl { max-width: 56rem; }
  .max-w-6xl { max-width: 72rem; }

  .mx-auto { margin-left: auto; margin-right: auto; }
  .mb-2 { margin-bottom: 0.5rem; }
  .mb-3 { margin-bottom: 0.75rem; }
  .mb-4 { margin-bottom: 1rem; }
  .mb-6 { margin-bottom: 1.5rem; }
  .mb-8 { margin-bottom: 2rem; }
  .mb-12 { margin-bottom: 3rem; }
  .mb-16 { margin-bottom: 4rem; }
  .mb-24 { margin-bottom: 6rem; }
  .mt-2 { margin-top: 0.5rem; }

  .px-4 { padding-left: 1rem; padding-right: 1rem; }
  .px-6 { padding-left: 1.5rem; padding-right: 1.5rem; }
  .px-8 { padding-left: 2rem; padding-right: 2rem; }
  .py-2 { padding-top: 0.5rem; padding-bottom: 0.5rem; }
  .py-4 { padding-top: 1rem; padding-bottom: 1rem; }
  .py-6 { padding-top: 1.5rem; padding-bottom: 1.5rem; }
  .py-24 { padding-top: 6rem; padding-bottom: 6rem; }
  .pt-32 { padding-top: 8rem; }
  .pb-24 { padding-bottom: 6rem; }

  .p-6 { padding: 1.5rem; }

  .text-sm { font-size: 0.875rem; line-height: 1.25rem; }
  .text-base { font-size: 1rem; line-height: 1.5rem; }
  .text-lg { font-size: 1.125rem; line-height: 1.75rem; }
  .text-xl { font-size: 1.25rem; line-height: 1.75rem; }
  .text-3xl { font-size: 1.875rem; line-height: 2.25rem; }
  .text-4xl { font-size: 2.25rem; line-height: 2.5rem; }

  .font-medium { font-weight: 500; }
  .font-semibold { font-weight: 600; }
  .font-bold { font-weight: 700; }

  .leading-relaxed { line-height: 1.625; }
  .leading-tight { line-height: 1.25; }
  .tracking-tight { letter-spacing: -0.025em; }

  .text-center { text-align: center; }

  .text-gray-900 { color: #111827; }

  .bg-gradient-to-r { background-image: linear-gradient(to right, var(--tw-gradient-stops)); }
  .bg-gradient-to-b { background-image: linear-gradient(to bottom, var(--tw-gradient-stops)); }
  .bg-gradient-to-br { background-image: linear-gradient(to bottom right, var(--tw-gradient-stops)); }

  .from-volt { --tw-gradient-from: var(--color-volt); --tw-gradient-to: rgba(87, 253, 107, 0); --tw-gradient-stops: var(--tw-gradient-from), var(--tw-gradient-to); }
  .to-emerald-400 { --tw-gradient-to: #4ade80; }
  .via-transparent { --tw-gradient-to: rgba(87, 253, 107, 0); --tw-gradient-stops: var(--tw-gradient-from), transparent, var(--tw-gradient-to); }

  .bg-clip-text { background-clip: text; -webkit-background-clip: text; }
  .text-transparent { color: transparent; }

  .border { border-width: 1px; }
  .border-2 { border-width: 2px; }

  .rounded-full { border-radius: 9999px; }
  .rounded-lg { border-radius: 0.5rem; }
  .rounded-xl { border-radius: 0.75rem; }

  .shadow-lg { box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); }

  .backdrop-blur-sm { backdrop-filter: blur(4px); }

  .blur-3xl { filter: blur(64px); }

  .opacity-50 { opacity: 0.5; }

  .overflow-hidden { overflow: hidden; }

  .transform { transform: translateX(var(--tw-translate-x, 0)) translateY(var(--tw-translate-y, 0)) rotate(var(--tw-rotate, 0)) skewX(var(--tw-skew-x, 0)) skewY(var(--tw-skew-y, 0)) scaleX(var(--tw-scale-x, 1)) scaleY(var(--tw-scale-y, 1)); }
  .-translate-x-1\/2 { --tw-translate-x: -50%; }

  .transition-all { transition-property: all; transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); transition-duration: 150ms; }
  .transition-colors { transition-property: color, background-color, border-color, text-decoration-color, fill, stroke; transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); transition-duration: 150ms; }
  .transition-transform { transition-property: transform; transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); transition-duration: 150ms; }

  .duration-300 { transition-duration: 300ms; }

  .hover\:scale-110:hover { --tw-scale-x: 1.1; --tw-scale-y: 1.1; }
  .hover\:translate-x-0\.5:hover { --tw-translate-x: 0.125rem; }

  .group:hover .group-hover\:scale-110 { --tw-scale-x: 1.1; --tw-scale-y: 1.1; }
  .group:hover .group-hover\:translate-x-0\.5 { --tw-translate-x: 0.125rem; }

  .hover\:from-volt\/90:hover { --tw-gradient-from: rgba(87, 253, 107, 0.9); }
  .hover\:to-emerald-400\/90:hover { --tw-gradient-to: rgba(74, 222, 128, 0.9); }

  .hover\:bg-gradient-to-r:hover { background-image: linear-gradient(to right, var(--tw-gradient-stops)); }
  .hover\:from-volt\/10:hover { --tw-gradient-from: rgba(87, 253, 107, 0.1); }
  .hover\:to-emerald-400\/10:hover { --tw-gradient-to: rgba(74, 222, 128, 0.1); }

  .hover\:border-volt\/40:hover { border-color: rgba(87, 253, 107, 0.4); }

  .hover\:shadow-volt\/25:hover { box-shadow: 0 0 20px rgba(87, 253, 107, 0.25); }

  .flex-col { flex-direction: column; }
  .flex-wrap { flex-wrap: wrap; }
  .items-center { align-items: center; }
  .justify-center { justify-content: center; }
  .justify-between { justify-content: space-between; }

  .gap-2 { gap: 0.5rem; }
  .gap-3 { gap: 0.75rem; }
  .gap-6 { gap: 1.5rem; }
  .gap-8 { gap: 2rem; }
  .gap-12 { gap: 3rem; }
  .gap-16 { gap: 4rem; }

  .space-y-4 > * + * { margin-top: 1rem; }
  .space-y-6 > * + * { margin-top: 1.5rem; }
  .space-y-8 > * + * { margin-top: 2rem; }

  .grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }

  /* Media queries */
  @media (min-width: 640px) {
    .sm\:flex-row { flex-direction: row; }
  }

  @media (min-width: 768px) {
    .md\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .md\:grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .md\:text-4xl { font-size: 2.25rem; line-height: 2.5rem; }
    .md\:text-6xl { font-size: 3.75rem; line-height: 1; }
    .md\:text-xl { font-size: 1.25rem; line-height: 1.75rem; }
    .md\:hidden { display: none; }
    .md\:block { display: block; }
    .md\:flex { display: flex; }
  }

  @media (min-width: 1024px) {
    .lg\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .lg\:grid-cols-4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    .lg\:text-7xl { font-size: 4.5rem; line-height: 1; }
    .lg\:px-24 { padding-left: 6rem; padding-right: 6rem; }
  }

  .delay-300 { animation-delay: 300ms; }
  .delay-600 { animation-delay: 600ms; }
</style> 